import React from 'react'

export const NoItemSelected = (props) =>
    <>
        <h1>No {props.itemName} selected is selected. Select an item in the list to the left to get details.</h1>
    </>
